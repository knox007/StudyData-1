#include <stdio.h>

void main(){

	// 덧셈, 뺄셈, 곱셈, 나눗셈 중에 원하는 기호를 선택하여
	// a, b의 연산 결과값 출력하기. 
	/*
	char ch;
	int a = 10, b = 5;

	printf("a와 b의 연산을 선택하세요(+ - * /)\n");
	scanf("%c", &ch);

	switch(ch){
	case '+':
		printf("%d + %d = %d\n", a, b, a+b);
		break;
	case '-':
		printf("%d - %d = %d\n", a, b, a-b);
		break;
	case '*':
		printf("%d * %d = %d\n", a, b, a*b);
		break;
	case '/':
		printf("%d / %d = %d\n", a, b, a/b);
		break;
	default:
		printf("잘못된 기호를 적으셨어요\n");
	}*/

	// 문제:0~100사이 점수를 입력받고, 아래처럼 출력해보세요 
	//		90~100 == 수
	//		80~89 == 우
	//		70~79 == 미
	//		60~69 == 양
	//		그 이하 == 가

	